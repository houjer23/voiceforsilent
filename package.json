{
  "name": "voiceforsilence",
  "version": "1.0.0",
  "description": "A blog platform with Netlify Functions backend",
  "main": "index.js",
  "scripts": {
    "build": "node scripts/build.js",
    "migrate": "node scripts/migrate.js",
    "update-authors": "node scripts/update-authors.js",
    "dev": "netlify dev",
    "deploy": "npm run build && netlify deploy --prod --dir=dist",
    "start": "netlify dev"
  },
  "keywords": ["blog", "netlify", "neon", "postgresql"],
  "author": "VoiceForSilence Team",
  "license": "MIT",
  "dependencies": {
    "@neondatabase/serverless": "^0.10.5",
    "csv-parse": "^5.5.6",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "netlify-cli": "^17.37.3"
  }
}
